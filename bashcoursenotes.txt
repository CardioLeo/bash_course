# udemy course

# shebang line is #!, tells shell that it will be using Bash to interpret the script.

# Beginning:
# #!/bin/bash
# Middle:
# commands
# End:
# (exit code): "exit 0"

# that is, all bash scripts must have /bin/bash right after the shebang line.

# the exit codes are just written "exit 0"

# author, date of creation, 

# *** Scripts have to be in a bash_course file in the home directory in order to work correctly.
# *** I don't know where else they are supposed to be referenced from.
# *** Also, you have to remember to "sudo chmod 744 <script.name>" in order for them to work.
